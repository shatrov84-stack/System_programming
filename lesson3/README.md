Игорь, [06.01.2026 18:15]
Урок 3: Процессы (Chapter 3: Processes)

Цель урока: Понимать жизненный цикл, структуру и механизмы управления процессами в Windows. Научиться создавать, настраивать, перечислять и анализировать процессы с помощью WinAPI.

---

1. Создание и управление процессами

Основная функция для создания процесса — CreateProcess. Она позволяет запустить исполняемый файл с заданными параметрами.

Ключевые аспекты:

· Запуск приложения: Базовый вызов CreateProcess для запуска, например, калькулятора.
· Ожидание завершения: Использование функции WaitForSingleObject для синхронизации родительского и дочернего процессов.
· Получение информации: Анализ кода завершения процесса через GetExitCodeProcess.

Пример кода (Базовое создание процесса):

#include <windows.h>
#include <tchar.h>
#include <stdio.h>

int main() {
    STARTUPINFO si = { sizeof(si) };
    PROCESS_INFORMATION pi;
    TCHAR cmdLine[] = _T("notepad.exe");

    // Создаём процесс
    if (!CreateProcess(
        NULL,           // Имя приложения (указываем в командной строке)
        cmdLine,        // Командная строка
        NULL,           // Атрибуты безопасности процесса (по умолчанию)
        NULL,           // Атрибуты безопасности потока (по умолчанию)
        FALSE,          // Наследование дескрипторов (не наследуем)
        0,              // Флаги создания (обычный приоритет)
        NULL,           // Окружение (используем родительское)
        NULL,           // Текущий каталог (используем родительский)
        &si,            // STARTUPINFO
        &pi             // PROCESS_INFORMATION (получаем PID и дескрипторы)
    )) {
        _tprintf(_T("CreateProcess failed! Error: %d\n"), GetLastError());
        return 1;
    }

    _tprintf(_T("Process created! PID: %d\n"), pi.dwProcessId);

    // Ждём завершения процесса (можно закомментировать для асинхронной работы)
    WaitForSingleObject(pi.hProcess, INFINITE);

    DWORD exitCode;
    GetExitCodeProcess(pi.hProcess, &exitCode);
    _tprintf(_T("Process exited with code: %d\n"), exitCode);

    // Важно! Закрываем дескрипторы
    CloseHandle(pi.hProcess);
    CloseHandle(pi.hThread);

    return 0;
}
---

2. Атрибуты процесса и структуры данных

STARTUPINFO и STARTUPINFOEX
Содержат параметры для первоначального окна процесса (заголовок, положение, размер) и дескрипторы стандартных потоков ввода/вывода.

· Важное поле: dwFlags — определяет, какие поля структуры активны.
· Расширенная версия: STARTUPINFOEX позволяет через lpAttributeList задавать наследуемые дескрипторы и политики безопасности.

PROCESS_INFORMATION
Возвращаемая структура, содержащая:

· hProcess — дескриптор нового процесса.
· hThread — дескриптор его основного потока.
· dwProcessId и dwThreadId — их идентификаторы (PID, TID). Важно: PID может быть переиспользован системой после завершения процесса, в отличие от уникального дескриптора.

Пример настройки атрибутов:

// Настройка STARTUPINFO для перенаправления вывода
STARTUPINFO si = { sizeof(si) };
SECURITY_ATTRIBUTES sa = { sizeof(sa), NULL, TRUE }; // Наследуемый дескриптор

HANDLE hStdOut = CreateFile(_T("output.log"), ...);
si.hStdOutput = hStdOut;
si.dwFlags = STARTF_USESTDHANDLES; // Говорим системе использовать наши дескрипторы

// Затем передаём &si в CreateProcess
---

3. Переменные окружения

Каждый процесс имеет блок окружения — набор строк вида VAR=Value, используемый для конфигурации.

Основные API функции:

· GetEnvironmentVariable — получить значение конкретной переменной.
· SetEnvironmentVariable — установить или удалить переменную для текущего процесса.
· GetEnvironmentStrings / FreeEnvironmentStrings — получить весь блок для перебора.

Пример работы с окружением:

`c
TCHAR buffer[256];
DWORD len = GetEnvironmentVariable(_T("PATH"), buffer, 256);
if (len > 0) {
    _tprintf(_T("PATH is: %s\n"), buffer);
} else if (GetLastError() == ERROR_ENVVAR_NOT_FOUND) {
    _tprintf(_T("Variable not found.\n"));
}

// Установка переменной (действует только для текущего процесса и его детей)
SetEnvironmentVariable(_T("MY_APP_MODE"), _T("DEBUG"));

Игорь, [06.01.2026 18:15]
// Перебор всех переменных окружения
LPTCH env = GetEnvironmentStrings();
for (LPTCH var = env; *var; var += _tcslen(var) + 1) {
    _tprintf(_T("%s\n"), var);
}
FreeEnvironmentStrings(env);

Совет: Переменные, установленные через SetEnvironmentVariable перед вызовом CreateProcess, могут быть унаследованы дочерним процессом, если это разрешено.

---

4. Защищенные процессы (PPL — Protected Process Light)

PPL — механизм защиты критических системных процессов (например, антивирусов) от вмешательства извне, даже от администратора.

Ключевые понятия:

· Уровень защиты (ProtectionLevel): Определяется в атрибутах процесса (PROC_THREAD_ATTRIBUTE_PROTECTION_LEVEL) через STARTUPINFOEX.
· Подписанты: Процесс должен быть подписан определённым сертификатом (Windows, антивирус и т.д.).
· Ограничения: PPL-процесс может быть открыт на запись только процессом с более высоким или равным уровнем защиты.

Пример создания PPL (требуются права и подпись):

c
// Упрощённая схема. На практике требует подписанный бинарник и права.
STARTUPINFOEX siex = { sizeof(siex) };
PROC_THREAD_ATTRIBUTE_LIST* pattrList = NULL;
SIZE_T size = 0;

// 1. Вычисляем размер нужен для списка атрибутов
InitializeProcThreadAttributeList(NULL, 1, 0, &size);
pattrList = (PROC_THREAD_ATTRIBUTE_LIST*)HeapAlloc(GetProcessHeap(), 0, size);

// 2. Инициализируем список
InitializeProcThreadAttributeList(pattrList, 1, 0, &size);

// 3. Устанавливаем атрибут уровня защиты (например, WinTcb)
DWORD protectionLevel = PROTECTION_LEVEL_WINTCB;
UpdateProcThreadAttribute(pattrList, 0,
                          PROC_THREAD_ATTRIBUTE_PROTECTION_LEVEL,
                          &protectionLevel,
                          sizeof(protectionLevel),
                          NULL, NULL);

siex.lpAttributeList = pattrList;
siex.StartupInfo.cb = sizeof(siex);

// 4. Создаём процесс с флагом PROTECTED_PROCESS
CreateProcess(..., CREATE_PROTECTED_PROCESS, ..., &siex.StartupInfo, ...);

// 5. Освобождаем список
DeleteProcThreadAttributeList(pattrList);
HeapFree(GetProcessHeap(), 0, pattrList);

---

5. Перечисление процессов

Снимок системы (Toolhelp Snapshot) — основной способ получить список процессов.

Последовательность действий:

1. Создать снимок процессов и потоков (CreateToolhelp32Snapshot).
2. Получить информацию о первом процессе (Process32First).
3. В цикле получать информацию о последующих процессах (Process32Next).
4. Закрыть дескриптор снимка.

Пример перечисления процессов:

c
#include <tlhelp32.h> // Для функций Toolhelp

void ListProcesses() {
    HANDLE hSnapshot = CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0);
    if (hSnapshot == INVALID_HANDLE_VALUE) return;

    PROCESSENTRY32 pe32 = { sizeof(PROCESSENTRY32) };

    if (Process32First(hSnapshot, &pe32)) {
        _tprintf(_T("PID\t\tProcess Name\n"));
        _tprintf(_T("====================\n"));
        do {
            _tprintf(_T("%-8u\t%s\n"), pe32.th32ProcessID, pe32.szExeFile);
        } while (Process32Next(hSnapshot, &pe32));
    } else {
        _tprintf(_T("Failed to get process info. Error: %d\n"), GetLastError());
    }

    CloseHandle(hSnapshot);
}
`

Альтернативные методы:

· Функции PSAPI (EnumProcesses): Проще, но даёт только PID.
· WMI (COM-интерфейс): Максимально подробная информация, но сложнее в использовании.

---

📌 Практическое задание

1. Модифицируйте пример создания процесса так, чтобы:
   · Блокировка родительского процесса происходила не более чем на 5 секунд (используйте WaitForSingleObject с таймаутом).
   · В случае успешного завершения выводилось "Process finished normally", в случае таймаута — "Process is still running!".
2. Напишите утилиту envviewer, которая:
   · Принимает имя переменной окружения как аргумент командной строки.
   · Если аргумент передан — выводит её значение.

Игорь, [06.01.2026 18:15]
· Если аргумента нет — выводит все переменные окружения текущего процесса.
3. Создайте "Монитор процессов":
   · Реализуйте функцию ListProcesses, как показано выше.
   · Добавьте возможность поиска процесса по имени (части имени) и вывода его PID.
   · Дополнительно: Попробуйте получить для каждого процесса имя его родителя (th32ParentProcessID) и преобразовать PID в имя, используя второй снимок или кэш.

---

🔍 Что важно запомнить

· Дескрипторы — это ресурсы! Всегда закрывайте hProcess и hThread, полученные из CreateProcess.
· PID ≠ Дескриптор. Идентификатор (PID) может быть переиспользован системой. Для уникальной идентификации в долгоживущих приложениях используйте дескрипторы с дублированием (DuplicateHandle).
· Окружение наследуется. Переменные, установленные в родительском процессе, видны дочернему, если не указано иное.
· Снимки — это моментальное состояние. Информация в PROCESSENTRY32 актуальна только на момент создания снимка. Процесс может уже завершиться.
· PPL — это не магия. Это строгая политика безопасности, требующая подписанного кода и определённых привилегий.

Материалы этого урока — фундамент для понимания заданий (Jobs), межпроцессного взаимодействия и безопасности, которые будут рассматриваться далее.
