# 🎯 **УРОК 1: СОЗДАНИЕ ОКНА WINDOWS. ВАШЕ ПЕРВОЕ ГРАФИЧЕСКОЕ ПРИЛОЖЕНИЕ**

**Цель урока:**  
Создать первое оконное приложение Windows с помощью WinAPI. Понять архитектуру Windows-программ: цикл сообщений, оконную процедуру и регистрацию классов окон.

---
## 📚 **ВИДЕО:**
https://vk.com/away.php?to=https%3A%2F%2Fyoutu.be%2FGgWk1b2pXYA%3Fsi%3DTbscENxE8mw5ijAN&utf=1

## 📚 **ТЕОРЕТИЧЕСКИЕ МАТЕРИАЛЫ:**

### 1. **Ключевые отличия Windows-программ:**
- **`WinMain`** вместо `main` — специальная точка входа для GUI-приложений
- **Событийно-ориентированная модель** — программа реагирует на сообщения
- **Нет прямого вывода** — рисуем в окне через контекст устройства (HDC)

### 2. **Основные компоненты Windows-программы:**
- **`HWND`** — дескриптор окна (уникальный ID окна в системе)
- **`WNDCLASS`** — структура-шаблон, описывающая класс окна
- **Цикл сообщений** — бесконечный цикл получения и обработки событий
- **`WindowProc`** — функция-обработчик всех сообщений окна

### 3. **Ключевые сообщения Windows:**
- **`WM_PAINT`** — "перерисуй окно" (вызывается при первой отрисовке, изменении размера и т.д.)
- **`WM_DESTROY`** — "окно закрывается" (завершаем программу)
- **`WM_CREATE`** — "окно создано" (инициализация)
- **`WM_SIZE`** — "окно изменило размер"

### 4. **Архитектура Windows-приложения:**
```
wWinMain() → RegisterClass() → CreateWindowEx() → ShowWindow() → Цикл сообщений
     ↓               ↓               ↓               ↓              ↓
Точка входа  Регистрация    Создание окна   Показ окна   GetMessage() → WindowProc()
             класса окна                                   ↑
                                                        Обработка
                                                        сообщений
```

---

## 📝 **ЗАДАНИЕ ДЛЯ ВАС:**

### **Часть 1: Теория (ответьте письменно 2-3 предложениями)**
1. Почему Windows-программы используют `WinMain` вместо обычного `main`?
2. Что происходит в функции `WindowProc` и почему она так важна?
3. Зачем нужен цикл `while(GetMessage(...))`? Что произойдет, если его убрать?

### **Часть 2: Практика (основное задание)**

**Задача:** Создайте и запустите ваше первое оконное приложение Windows.

**Что делает программа:**
1. Регистрирует класс окна в системе Windows
2. Создает реальное окно с заголовком и рамкой
3. Запускает цикл обработки сообщений
4. Рисует текст внутри окна при получении `WM_PAINT`
5. Корректно закрывается при получении `WM_DESTROY`

**Полный код программы:**
```c
#include <windows.h>

// Прототип оконной процедуры
LRESULT CALLBACK WindowProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);

// Точка входа
int WINAPI wWinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance,
    PWSTR pCmdLine, int nCmdShow)
{
    // 1. РЕГИСТРИРУЕМ КЛАСС ОКНА
    const wchar_t CLASS_NAME[] = L"MyWindowClass";

    WNDCLASS wc = { 0 };
    wc.lpfnWndProc = WindowProc;
    wc.hInstance = hInstance;
    wc.lpszClassName = CLASS_NAME;
    wc.hCursor = LoadCursor(NULL, IDC_ARROW);
    wc.hbrBackground = (HBRUSH)(COLOR_WINDOW + 1);

    RegisterClass(&wc);

    // 2. СОЗДАЕМ ОКНО
    HWND hwnd = CreateWindowEx(
        0,
        CLASS_NAME,
        L"Урок 1: Моё первое окно",
        WS_OVERLAPPEDWINDOW,
        CW_USEDEFAULT, CW_USEDEFAULT, 500, 300,
        NULL,
        NULL,
        hInstance,
        NULL
    );

    if (hwnd == NULL) {
        return 0;
    }

    // 3. ПОКАЗЫВАЕМ ОКНО
    ShowWindow(hwnd, nCmdShow);

    // 4. ЦИКЛ СООБЩЕНИЙ
    MSG msg = { 0 };
    while (GetMessage(&msg, NULL, 0, 0)) {
        TranslateMessage(&msg);
        DispatchMessage(&msg);
    }

    return 0;
}

// ФУНКЦИЯ ОБРАБОТКИ ОКНА
LRESULT CALLBACK WindowProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam)
{
    switch (uMsg)
    {
    case WM_PAINT:
    {
        PAINTSTRUCT ps;
        HDC hdc = BeginPaint(hwnd, &ps);

        // Очищаем фон окна
        FillRect(hdc, &ps.rcPaint, (HBRUSH)(COLOR_WINDOW + 1));

        // Выводим текст
        TextOutW(hdc, 50, 50, L"Поздравляю! Вы создали:", 26);
        TextOutW(hdc, 70, 80, L"1. Настоящее окно Windows", 26);
        TextOutW(hdc, 70, 110, L"2. С обработкой сообщений", 26);
        TextOutW(hdc, 70, 140, L"3. С возможностью рисования", 28);

        TextOutW(hdc, 50, 180, L"Закройте это окно для завершения.", 33);

        EndPaint(hwnd, &ps);
        return 0;
    }

    case WM_DESTROY:
        PostQuitMessage(0);
        return 0;
    }

    return DefWindowProc(hwnd, uMsg, wParam, lParam);
}
```

---

## 🔧 **КАК СКОМПИЛИРОВАТЬ И ЗАПУСТИТЬ:**

### **Способ 1: Visual Studio (рекомендуется)**
1. Сохраните код как `window.c`
2. Откройте **Developer Command Prompt for VS**
3. Выполните команду:
```bash
cl window.c user32.lib gdi32.lib /link /subsystem:windows /out:mywindow.exe
```

### **Способ 2: MinGW/GCC**
```bash
gcc window.c -o mywindow.exe -lgdi32 -luser32 -mwindows
```

### **Что должно получиться:**
- Запустится файл `mywindow.exe`
- Откроется окно с белым фоном и текстом
- Окно можно перемещать, изменять размер, минимизировать
- При закрытии окна программа завершится

---

## 🎯 **ЗАДАНИЯ ДЛЯ САМОСТОЯТЕЛЬНОЙ РАБОТЫ:**

### **Базовый уровень:**
1. Измените заголовок окна на "Моя первая программа"
2. Измените размер окна на 600x400 пикселей
3. Добавьте вывод текущей даты в окно (используйте `GetLocalTime`)

### **Продвинутый уровень:**
1. Измените цвет фона окна (подсказка: измените `COLOR_WINDOW + 1`)
2. Добавьте обработку клика мыши (`WM_LBUTTONDOWN`)
3. Сделайте так, чтобы при клике выводились координаты клика

**Код для получения времени:**
```c
SYSTEMTIME st;
GetLocalTime(&st);
wchar_t dateStr[100];
swprintf(dateStr, 100, L"Дата: %02d.%02d.%d", 
         st.wDay, st.wMonth, st.wYear);
TextOutW(hdc, 50, 200, dateStr, wcslen(dateStr));
```

**Код для обработки клика мыши:**
```c
case WM_LBUTTONDOWN: {
    int x = LOWORD(lParam);
    int y = HIWORD(lParam);
    // Вывести координаты в окно
    break;
}
```

---

## 🔍 **НА ЧТО Я ОБРАЩУ ВНИМАНИЕ ПРИ ПРОВЕРКЕ:**

1. **Корректность архитектуры:** Наличие всех обязательных компонентов (WinMain, RegisterClass, цикл сообщений, WindowProc)
2. **Обработка сообщений:** Правильная обработка `WM_PAINT` и `WM_DESTROY`
3. **Качество кода:** Понятные комментарии, структурированный код
4. **Дополнительная функциональность:** Выполнение заданий из раздела "Самостоятельная работа"

---

## 💡 **ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ:**

### **Ошибка: "неразрешенный внешний символ _main"**
**Решение:** Укажите правильную точку входа при компиляции или настройте проект как Windows-приложение.

### **Ошибка: Окно появляется и сразу исчезает**
**Решение:** Вы запускаете программу из командной строки. Запустите `.exe` файл двойным кликом.

### **Ошибка: Текст не отображается или крякозябры**
**Решение:** Используйте Unicode-версии функций (`TextOutW`, `wchar_t`).

---

## 📚 **ПОЛНЫЙ ПЛАН КУРСА:**

### ✅ **Урок 1:** Создание окна Windows (текущий урок)
### 🔜 **Урок 2:** Добавление элементов управления (кнопки, меню)
### 🔜 **Урок 3:** Работа с файловой системой
### 🔜 **Урок 4:** Диалоговые окна и ввод данных
### 🔜 **Урок 5:** Процессы и многопоточность
### 🔜 **Урок 6:** Финальный проект — системная утилита

---

## 📞 **КАК ОТПРАВИТЬ РЕШЕНИЕ:**

Пришлите, пожалуйста:
1. **Ответы на теоретические вопросы** (3 вопроса из начала урока)
2. **Исходный код вашей программы** (файл `.c` или `.cpp`)
3. **Скриншот работающего окна** (или описание, что получилось)
4. **Комментарии:** С какими сложностями столкнулись? Что было непонятно?

**Срок выполнения:** 3-5 дней  
**Обратная связь:** В течение 24 часов после получения работы
