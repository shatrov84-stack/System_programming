# 🛠️ **ПОДГОТОВИТЕЛЬНЫЙ БЛОК: ИНСТРУМЕНТЫ И ОСНОВЫ СИСТЕМНОГО ПРОГРАММИСТА**

**Цель блока:**  
Настроить профессиональное окружение и освоить ключевые инструменты разработки, компиляции и отладки под Windows. Заложить фундамент для успешного освоения системного программирования.

---

## 🎯 **ЗАНЯТИЕ П1: НАСТРОЙКА ПРОФЕССИОНАЛЬНОГО ОКРУЖЕНИЯ WINDOWS**

**Цель:** Настроить удобную, мощную и изолированную среду разработки на Windows, используя современные системные инструменты и практики.

### 📚 **КЛЮЧЕВЫЕ ТЕМЫ:**

#### **1. Windows Terminal — терминал нового поколения:**
- **Установка:** Через Microsoft Store или WinGet
- **Настройка профилей:** PowerShell, CMD, WSL, Azure Cloud Shell
- **Визуальное оформление:** Темы, шрифты, прозрачность, разделение панелей
- **Горячие клавиши:** Быстрая навигация, поиск, копирование

#### **2. WSL 2 — Linux внутри Windows:**
- **Установка:** `wsl --install -d Ubuntu`
- **Файловая система:** Доступ к Windows файлам (`/mnt/c/`), интеграция с VS Code
- **Инструменты:** GCC, GDB, Make, Git, Python, SSH
- **Сетевые возможности:** Localhost forwarding, VPN совместимость

#### **3. WinGet — менеджер пакетов от Microsoft:**
- **Поиск пакетов:** `winget search <имя>`
- **Установка:** `winget install <id>`
- **Обновление:** `winget upgrade --all`
- **Популярные пакеты:** Git, VS Code, Python, Node.js, Docker Desktop

#### **4. PowerToys — набор утилит для мощности:**
- **PowerToys Run:** Быстрый запуск приложений (Alt+Space)
- **FancyZones:** Организация окон на сетке (Win+Shift+`)
- **PowerRename:** Пакетное переименование файлов
- **Color Picker:** Захват цвета с экрана (Win+Shift+C)

#### **5. Организация рабочего пространства:**
- **Структура каталогов:** `%USERPROFILE%\source`, `D:\Dev`, `C:\Projects`
- **Переменные окружения:** PATH, INCLUDE, LIB, TEMP
- **Символьные ссылки:** `mklink /D` для организации проектов
- **Резервное копирование:** OneDrive, Git, внешние диски

---

### 📝 **ПРАКТИЧЕСКОЕ ЗАДАНИЕ:**

#### **Задача 1: Windows Terminal**
```powershell
# Установка через Microsoft Store (рекомендуется)
# Или через WinGet:
winget install Microsoft.WindowsTerminal

# Настройка профилей в settings.json:
# - Изменить цветовую схему на "One Half Dark"
# - Добавить шрифт "Cascadia Code PL"
# - Настроить прозрачность 85%
```

#### **Задача 2: WSL 2**
```powershell
# Включение компонентов Windows
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# Установка WSL 2
wsl --install -d Ubuntu

# Проверка установки
wsl --list --verbose
wsl --status
```

#### **Задача 3: WinGet установка инструментов**
```powershell
# Основные инструменты разработчика
winget install Git.Git
winget install Microsoft.VisualStudioCode
winget install Python.Python.3.11
winget install Microsoft.PowerToys
winget install Microsoft.VisualStudio.2022.Community --override "--add Microsoft.VisualStudio.Workload.NativeDesktop"

# Дополнительные утилиты
winget install Microsoft.Sysinternals.ProcessExplorer
winget install Microsoft.WindowsADK
winget install Ollama.Ollama
```

#### **Задача 4: Организация рабочего пространства**
```powershell
# Создание структуры каталогов
New-Item -ItemType Directory -Force -Path "D:\Dev"
New-Item -ItemType Directory -Force -Path "D:\Dev\cpp"
New-Item -ItemType Directory -Force -Path "D:\Dev\cpp\system_prog"
New-Item -ItemType Directory -Force -Path "D:\Dev\cpp\system_prog\projects"
New-Item -ItemType Directory -Force -Path "D:\Dev\cpp\system_prog\libs"
New-Item -ItemType Directory -Force -Path "D:\Dev\cpp\system_prog\tools"

# Символьные ссылки для быстрого доступа
cmd /c mklink /D "%USERPROFILE%\dev" "D:\Dev"
```

#### **Задача 5: Настройка PowerToys**
1. Откройте **PowerToys Settings**
2. Включите **FancyZones** и создайте сетку 3×3
3. Активируйте **PowerToys Run** (Alt+Space)
4. Настройте **Color Picker** для быстрого захвата цветов
5. Включите **File Explorer add-ons** для Preview панели

---

### ✅ **КРИТЕРИИ ВЫПОЛНЕНИЯ:**

1. **Windows Terminal:** 3+ профиля, кастомная тема, разделение панелей
2. **WSL 2:** Установленный Ubuntu, доступ к Windows файлам
3. **WinGet:** 5+ установленных инструментов через командную строку
4. **PowerToys:** FancyZones с кастомной сеткой, PowerToys Run работает
5. **Структура:** Организованная иерархия каталогов для проектов

---

### 📋 **ЧЕК-ЛИСТ ПРОВЕРКИ:**

- [ ] Windows Terminal открывается по Ctrl+`
- [ ] В WSL выполняется `gcc --version`
- [ ] VS Code открывает файлы из WSL (`code .` в терминале Ubuntu)
- [ ] PowerToys Run вызывается по Alt+Space
- [ ] Git установлен и настроен (`git --version`)
- [ ] Структура каталогов создана и доступна

---

## 🛠️ **ЗАНЯТИЕ П2: МАСТЕРСТВО КОМПИЛЯЦИИ И СБОРКИ НАТИВНОГО КОДА**

**Цель:** Научиться управлять процессом компиляции и сборки нативного C/C++ кода в Windows, понимать ключевые параметры компилятора и создавать отлаживаемые исполняемые файлы.

### 📚 **КЛЮЧЕВЫЕ ТЕМЫ:**

#### **1. Visual Studio Build Tools:**
- **Компоненты:** cl.exe, link.exe, lib.exe, nmake.exe
- **Установка:** Минимальный набор для компиляции C++
- **Developer Command Prompt:** Автоматическая настройка окружения
- **Пути и переменные:** INCLUDE, LIB, PATH для MSVC

#### **2. Флаги компилятора MSVC (cl.exe):**
- **Отладка:**
  - `/DEBUG` — генерация отладочной информации
  - `/DEBUG:FULL` — полная отладочная информация (релиз)
  - `/DEBUG:FASTLINK` — быстрая линковка (разработка)
  - `/Zi` — создание PDB файла
- **Оптимизация:**
  - `/Od` — отключение оптимизации (отладка)
  - `/O1` — оптимизация по размеру
  - `/O2` — оптимизация по скорости
  - `/Ox` — максимальная оптимизация
- **Предупреждения:**
  - `/W0` — отключение предупреждений
  - `/W1` — базовые предупреждения
  - `/W3` — рекомендуемый уровень
  - `/W4` — все предупреждения
  - `/WX` — трактовать предупреждения как ошибки
- **Стандарт языка:**
  - `/std:c++20` — стандарт C++20
  - `/std:c++latest` — последние возможности
  - `/permissive-` — строгое соответствие стандарту

#### **3. Флаги линковщика (link.exe):**
- **Тип подсистемы:**
  - `/SUBSYSTEM:CONSOLE` — консольное приложение
  - `/SUBSYSTEM:WINDOWS` — оконное приложение
- **Защита:**
  - `/DYNAMICBASE` — ASLR (Address Space Layout Randomization)
  - `/GUARD:CF` — Control Flow Guard
  - `/NXCOMPAT` — Data Execution Prevention
- **Прочее:**
  - `/INCREMENTAL` — инкрементальная линковка
  - `/LTCG` — Link Time Code Generation

#### **4. PDB-файлы (Program Database):**
- **Структура:** Типы записей, символов, исходных файлов
- **Размещение:** Рядом с исполняемым файлом
- **Совместимость:** Версии PDB, работа с разными сборками
- **Инструменты:** PDBCOPY, DUMPBIN, DBH

#### **5. Типы компоновки:**
- **Статическая (`/MT`, `/MTd`):** Библиотеки включаются в EXE
- **Динамическая (`/MD`, `/MDd`):** Библиотеки загружаются из DLL
- **Сравнение:** Размер файла, зависимость от MSVCRT, обновления

---

### 📝 **ПРАКТИЧЕСКОЕ ЗАДАНИЕ:**

#### **Задача 1: Базовая компиляция**
```c
// hello.c
#include <stdio.h>

int main() {
    printf("Hello, System Programming!\n");
    return 0;
}
```

```batch
:: Компиляция и линковка вручную
cl /nologo /W4 /WX /c hello.c
link /nologo /DEBUG /SUBSYSTEM:CONSOLE hello.obj

:: Или одной командой
cl /nologo /W4 /WX /DEBUG /Fe:hello.exe hello.c
```

#### **Задача 2: Сравнение флагов оптимизации**
```c
// optimize_test.c
#include <stdio.h>
#include <time.h>

double compute() {
    double sum = 0;
    for (int i = 0; i < 1000000; i++) {
        sum += i * 0.5;
    }
    return sum;
}

int main() {
    clock_t start = clock();
    double result = compute();
    clock_t end = clock();
    
    printf("Result: %f\n", result);
    printf("Time: %f seconds\n", (double)(end - start) / CLOCKS_PER_SEC);
    return 0;
}
```

```batch
:: Сборка с разными флагами
cl /nologo /Od /Fe:debug.exe optimize_test.c    :: Без оптимизации
cl /nologo /O1 /Fe:size_opt.exe optimize_test.c :: Оптимизация по размеру
cl /nologo /O2 /Fe:speed_opt.exe optimize_test.c :: Оптимизация по скорости

:: Сравнение размеров
dir *.exe
```

#### **Задача 3: Работа с PDB файлами**
```batch
:: Создание разных типов PDB
cl /nologo /Zi /Fe:fastlink.exe hello.c        :: FASTLINK PDB
cl /nologo /DEBUG:FULL /Fe:fullpdb.exe hello.c :: FULL PDB

:: Просмотр информации о PDB
dumpbin /pdbpath:fastlink.exe
dumpbin /pdbpath:fullpdb.exe

:: Проверка отладочной информации
dumpbin /headers /pdbpath fastlink.exe
```

#### **Задача 4: Статическая vs Динамическая компоновка**
```batch
:: Статическая компоновка (включает CRT в EXE)
cl /nologo /MT /Fe:static.exe hello.c
dumpbin /dependents static.exe

:: Динамическая компоновка (использует MSVCRT.dll)
cl /nologo /MD /Fe:dynamic.exe hello.c
dumpbin /dependents dynamic.exe

:: Сравнение размеров
dir static.exe dynamic.exe
```

#### **Задача 5: Комплексный пример с makefile**
```makefile
# build.mak
CC = cl
CFLAGS = /nologo /W4 /WX /std:c++17
LDFLAGS = /DEBUG /SUBSYSTEM:CONSOLE

# Цели по умолчанию
all: debug release

# Отладочная сборка
debug: hello_debug.exe

hello_debug.exe: hello.cpp
	$(CC) $(CFLAGS) /Od /Zi /Fe:$@ $** $(LDFLAGS)

# Релизная сборка
release: hello_release.exe

hello_release.exe: hello.cpp
	$(CC) $(CFLAGS) /O2 /Fe:$@ $** /DEBUG:FULL

# Очистка
clean:
	del *.exe *.obj *.pdb *.ilk *.exp *.lib
```

```batch
:: Использование nmake
nmake /f build.mak
nmake /f build.mak clean
```

---

### 🧪 **ЭКСПЕРИМЕНТЫ ДЛЯ САМОСТОЯТЕЛЬНОЙ РАБОТЫ:**

1. **Влияние LTCG:**
   ```batch
   cl /O2 /GL hello.cpp           :: Включение Whole Program Optimization
   link /LTCG hello.obj           :: Link Time Code Generation
   ```

2. **Защитные механизмы:**
   ```batch
   cl /GS hello.cpp               :: Buffer Security Check
   link /DYNAMICBASE /NXCOMPAT /GUARD:CF hello.obj
   ```

3. **Разные подсистемы:**
   ```batch
   :: Консольное приложение
   cl /Fe:console.exe hello.cpp /link /SUBSYSTEM:CONSOLE
   
   :: Оконное приложение (без консоли)
   cl /Fe:windows.exe winhello.cpp /link /SUBSYSTEM:WINDOWS
   ```

4. **Работа с библиотеками:**
   ```batch
   :: Создание статической библиотеки
   lib /nologo /out:mylib.lib myobj1.obj myobj2.obj
   
   :: Использование библиотеки
   cl /Fe:app.exe main.cpp /link mylib.lib
   ```

---

### 📊 **АНАЛИТИЧЕСКАЯ ТАБЛИЦА:**

| Флаг | Размер EXE | Размер PDB | Скорость | Отладка | Описание |
|------|------------|------------|----------|---------|----------|
| `/Od` | Средний | Малый | Медленно | Отлично | Без оптимизации |
| `/O1` | Малый | Средний | Быстро | Хорошо | Оптимизация размера |
| `/O2` | Средний | Большой | Очень быстро | Плохо | Оптимизация скорости |
| `/MT` | Большой | - | - | - | Статическая CRT |
| `/MD` | Малый | - | - | - | Динамическая CRT |
| `/DEBUG:FASTLINK` | - | Малый | Быстро | Хорошо | Быстрая отладка |
| `/DEBUG:FULL` | - | Большой | Медленно | Отлично | Полная отладка |

---

## 🔍 **ЗАНЯТИЕ П3: ВВЕДЕНИЕ В ОТЛАДКУ И СИСТЕМНОЕ ПРОФИЛИРОВАНИЕ**

**Цель:** Освоить базовый инструментарий для диагностики проблем, анализа производительности и исследования поведения приложений и самой системы Windows.

### 📚 **КЛЮЧЕВЫЕ ТЕМЫ:**

#### **1. Системный монитор (PerfMon):**
- **Счетчики производительности:** Процессор, память, диск, сеть
- **Наборы сборщиков данных:** Создание, планирование, анализ
- **Базовые счетчики:**
  - `\Processor(_Total)\% Processor Time`
  - `\Memory\Available MBytes`
  - `\PhysicalDisk(_Total)\% Disk Time`
  - `\Process(*)\% Processor Time`

#### **2. Windows Performance Toolkit (WPT):**
- **WPR (Recorder):** Запись трассировок событий Windows (ETW)
- **WPA (Analyzer):** Анализ записанных трассировок
- **Ключевые графы:** CPU Usage, Disk I/O, Memory, Context Switches
- **Фильтрация:** По процессу, по стеку, по времени

#### **3. Набор Sysinternals:**
- **Process Explorer:** Расширенный диспетчер задач
- **Process Monitor:** Мониторинг файлов, реестра, процессов
- **Autoruns:** Управление автозагрузкой и службами
- **TCPView:** Сетевые соединения в реальном времени
- **Handle:** Поиск процессов, использующих файлы

#### **4. Дампы памяти и WinDbg:**
- **Типы дампов:** Минидамп, полный дамп, дамп ядра
- **Инструменты:** Procdump, Task Manager, WER
- **Анализ в WinDbg:**
  - `!analyze -v` — автоматический анализ
  - `k` — вывод стека вызовов
  - `dv` — локальные переменные
  - `!heap` — информация о куче

#### **5. Профилирование кода:**
- **Встроенные средства:** `/profile` флаг в MSVC
- **Внешние профайлеры:** Visual Studio Profiler, Very Sleepy, Intel VTune
- **Методы:** Выборка, инструментирование, трассировка

---

### 📝 **ПРАКТИЧЕСКОЕ ЗАДАНИЕ:**

#### **Задача 1: Мониторинг системы с PerfMon**
```powershell
# Создание набора сборщиков данных
logman create counter SystemPerf -o "C:\PerfLogs\system_perf.blg" -f bincirc -v mmddhhmm -max 250 -c "\Processor(*)\*" "\Memory\*" "\PhysicalDisk(*)\*" "\Network Interface(*)\*"

# Запуск мониторинга
logman start SystemPerf

# Остановка и просмотр
logman stop SystemPerf
perfmon
```

#### **Задача 2: Запись ETW трассировки**
```batch
:: Запись базовой трассировки
wpr -start GeneralProfile -filemode

:: Выполнение тестовой нагрузки
YourTestApplication.exe

:: Остановка записи
wpr -stop output.etl

:: Анализ в WPA
wpa output.etl
```

#### **Задача 3: Использование Process Monitor**
```batch
:: Запуск Process Monitor с фильтрами
procmon.exe /AcceptEula /Quiet /Minimized /Filter "ProcessName is YourApp.exe"

:: Сохранение лога в файл
procmon.exe /BackingFile log.pml

:: Анализ в Process Monitor
:: 1. Фильтр: Operation is ReadFile
:: 2. Фильтр: Path contains ".dll"
:: 3. Статистика по процессам
```

#### **Задача 4: Создание и анализ дампов**
```c
// crash_test.cpp
#include <windows.h>
#include <stdio.h>

void CrashFunction() {
    int* ptr = nullptr;
    *ptr = 42;  // Access violation
}

int main() {
    printf("Программа для тестирования дампов...\n");
    CrashFunction();
    return 0;
}
```

```batch
:: Компиляция тестовой программы
cl /nologo /Zi /Fe:crash_test.exe crash_test.cpp

:: Запуск и создание дампа при падении
procdump -ma -e -x . crash_test.exe

:: Анализ дампа в WinDbg
windbg -z crash_test.exe.dmp

:: В WinDbg выполнить:
!analyze -v
k
```

#### **Задача 5: Отладка "на лету"**
```batch
:: Поиск PID процесса
tasklist | findstr notepad

:: Присоединение WinDbg к процессу
windbg -p <PID>

:: Команды отладки:
bp kernel32!CreateFileW           :: Точка останова
g                                :: Продолжить выполнение
dv                               :: Локальные переменные
r                                :: Регистры
```

---

### 🔧 **ИНСТРУМЕНТАЛЬНЫЙ ШКАФЧИК:**

#### **Sysinternals в ежедневной работе:**
```powershell
# Автозагрузка системы
autorunsc.exe -accepteula > autoruns.csv

# Поиск блокирующих процессов
handle.exe -accepteula -a -u "C:\lockedfile.txt"

# Просмотр сетевых соединений
tcpview.exe -accepteula

# Анализ дискового пространства
du.exe -accepteula -q -l 2 C:\

# Мониторинг в реальном времени
procexp.exe -accepteula
```

#### **Сценарии диагностики:**

1. **Высокая загрузка CPU:**
   ```powershell
   # Запись трассировки во время пика
   wpr -start CPUProfile -filemode
   # ... ждем 30 секунд ...
   wpr -stop cpu_high.etl
   ```

2. **Утечка памяти:**
   ```batch
   procdump -ma -m 500 -n 3 YourApp.exe
   ```

3. **Медленный запуск приложения:**
   ```powershell
   procmon.exe /AcceptEula /Quiet /BackingFile app_start.pml
   # Запускаем приложение
   # Останавливаем запись и анализируем
   ```

---

### 📋 **ЧЕК-ЛИСТ ДИАГНОСТИКИ:**

#### **Когда что использовать:**
- **Зависло приложение** → Process Explorer (потоки, стек)
- **Не запускается программа** → Process Monitor (отсутствующие DLL, доступ)
- **Медленная работа** → PerfMon/WPR (CPU, Disk, Memory)
- **Падения и краши** → Procdump + WinDbg (дампы)
- **Сетевые проблемы** → TCPView (соединения)
- **Автозагрузка** → Autoruns (планировщик, службы)

#### **Типичные команды WinDbg:**
```
!analyze -v                     # Автоматический анализ
k                               # Стек текущего потока
~*k                             # Стеки всех потоков
!teb                            # TEB текущего потока
!peb                            # PEB процесса
!heap -s                        # Статистика кучи
lm                              # Загруженные модули
!address -summary               # Использование памяти
```

---

### 🎯 **КОМПЛЕКСНОЕ ЗАДАНИЕ:**

**Сценарий:** Приложение "DataProcessor.exe" работает медленно и периодически падает.

**Шаги диагностики:**

1. **Мониторинг ресурсов:**
   ```powershell
   logman create counter AppPerf -o "app_perf.blg" -c "\Process(DataProcessor)\*" "\Memory\*" -si 1
   logman start AppPerf
   ```

2. **Запись трассировки при падении:**
   ```batch
   procdump -ma -e -w DataProcessor.exe
   ```

3. **Анализ файловых операций:**
   ```powershell
   procmon.exe /AcceptEula /Quiet /BackingFile data_proc.pml /Filter "ProcessName is DataProcessor.exe"
   ```

4. **Диагностика в WinDbg:**
   ```batch
   windbg -z DataProcessor.exe.dmp
   !analyze -v
   !runaway      # Потребление CPU потоками
   !heap -s      # Статистика кучи
   ```

5. **Профилирование производительности:**
   ```batch
   wpr -start GeneralProfile -filemode
   # Запуск тестового сценария
   wpr -stop profile.etl
   ```

---

### 📊 **ШПАРГАЛКА ПО ИНСТРУМЕНТАМ:**

| Проблема | Первичный инструмент | Вторичный инструмент | Команда/Действие |
|----------|---------------------|---------------------|------------------|
| Высокая CPU | Process Explorer | WPR | Сортировка по CPU в Process Explorer |
| Утечка памяти | Process Explorer | WinDbg | Проверка Private Bytes в Process Explorer |
| Блокировка файла | Process Monitor | Handle | Фильтр по пути в Process Monitor |
| Сетевые соединения | TCPView | netstat | Просмотр в TCPView |
| Автозагрузка | Autoruns | regedit | Сканирование в Autoruns |
| Падения | Procdump | WinDbg | `procdump -ma -e app.exe` |
| Медленный запуск | Process Monitor | WPR | Мониторинг операций ввода-вывода |
| Отсутствующие DLL | Dependency Walker | Process Monitor | Проверка в Dependency Walker |

---

## 🏆 **ИТОГОВЫЙ ПРОЕКТ ПОДГОТОВИТЕЛЬНОГО БЛОКА**

### **Задача:** Создать "Диагностический набор системного программиста"

**Требования:**

1. **Установочный скрипт:** PowerShell скрипт, который устанавливает все необходимые инструменты
2. **Набор утилит:** Папка с портативными версиями ключевых инструментов
3. **Шаблоны конфигураций:** Готовые настройки для PerfMon, WPR, Process Monitor
4. **Примеры диагностики:** Тестовые приложения с типичными проблемами
5. **Документация:** Шпаргалка по командам и типовым сценариям

**Структура проекта:**
```
DiagnosticKit/
├── Install/
│   ├── Install-Tools.ps1
│   └── requirements.txt
├── Tools/
│   ├── Sysinternals/
│   ├── WPT/
│   └── WinDbg/
├── Configs/
│   ├── PerfMon/
│   ├── WPR/
│   └── ProcMon/
├── Samples/
│   ├── MemoryLeak.exe
│   ├── HighCPU.exe
│   └── CrashTest.exe
└── Docs/
    ├── QuickReference.md
    └── Scenarios.md
```

---

## 📈 **КРИТЕРИИ ОЦЕНКИ ПОДГОТОВИТЕЛЬНОГО БЛОКА:**

### **Базовый уровень:**
- ✅ Установлены и работают все основные инструменты
- ✅ Скомпилированы тестовые программы с разными флагами
- ✅ Выполнены базовые операции мониторинга

### **Продвинутый уровень:**
- ✅ Настроены кастомные конфигурации инструментов
- ✅ Проведена диагностика сложных сценариев
- ✅ Созданы скрипты автоматизации

### **Экспертный уровень:**
- ✅ Разработан комплексный диагностический набор
- ✅ Понимание взаимосвязей между инструментами
- ✅ Способность обучать других использованию инструментов

---

## 🚀 **ЧТО ДАЛЬШЕ?**

После освоения подготовительного блока вы готовы к:

1. **Основному курсу системного программирования:** Глубокое понимание процессов, потоков, памяти
2. **Продвинутой отладке:** Анализ дампов ядра, отладка драйверов
3. **Производительности:** Профилирование и оптимизация системного кода
4. **Безопасности:** Анализ уязвимостей, reverse engineering

**Подготовительный блок — это фундамент, на котором строится мастерство системного программиста. Хорошие инструменты не заменяют знания, но делают процесс обучения эффективнее и интереснее.**

---

## 📞 **ПОДДЕРЖКА И РЕСУРСЫ:**

### **Официальная документация:**
- [Windows Terminal Documentation](https://docs.microsoft.com/terminal)
- [WSL Documentation](https://docs.microsoft.com/wsl)
- [WinGet Documentation](https://docs.microsoft.com/windows/package-manager)
- [MSVC Compiler Options](https://docs.microsoft.com/cpp/build/reference/compiler-options)
- [WinDbg Documentation](https://docs.microsoft.com/windows-hardware/drivers/debugger)

### **Сообщество:**
- [Sysinternals Forum](https://forum.sysinternals.com)
- [Windows Dev Center](https://developer.microsoft.com/windows)
- [Stack Overflow: windbg](https://stackoverflow.com/questions/tagged/windbg)

### **Книги и курсы:**
- "Windows Internals" by Mark Russinovich
- "Advanced Windows Debugging" by Mario Hewardt
- "Writing Secure Code" by Michael Howard

**Удачи в освоении инструментов системного программиста! 🚀**
