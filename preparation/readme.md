Игорь, [06.01.2026 19:05]
Вот готовые материалы для трех подготовительных уроков в формате Markdown, которые можно напрямую скопировать в ваш README.md.

---

🛠️ Подготовительный блок: Инструменты и основы

Перед погружением в системное программирование необходимо настроить профессиональное окружение и освоить ключевые инструменты разработки, компиляции и отладки под Windows.

---

Занятие П1. Настройка профессионального окружения Windows

Цель: Настроить удобную, мощную и изолированную среду разработки на Windows, используя современные системные инструменты и практики.

Ключевые темы:

· Windows Terminal: Установка, настройка профилей (PowerShell, CMD, WSL) и внешнего вида. Основные команды для навигации.
· WSL (Windows Subsystem for Linux): Установка дистрибутива (например, Ubuntu). Базовое знакомство с файловой системой Linux, запуск Linux-утилит (gcc, gdb, make) из-под Windows.
· Управление пакетами через WinGet: Поиск, установка и обновление приложений (Git, VS Code, Python) через командную строку.
· Повышение эффективности с PowerToys: Использование PowerToys Run для быстрого запуска, FancyZones для организации окон, PowerRename для пакетного переименования.
· Организация рабочего пространства: Создание отдельного диска или раздела (D:\ или %USERPROFILE%\source) для всех проектов, исходного кода и инструментов. Настройка переменных окружения PATH.

Практическое задание:

1. Установите и настройте Windows Terminal, добавив профили для PowerShell и (опционально) WSL.
2. Установите WSL с дистрибутивом по выбору. Создайте в домашней директории WSL папку projects.
3. Используя WinGet, установите как минимум три новых инструмента (например, Git, VS Code, Python).
4. Настройте FancyZones для удобного расположения окон терминала, браузера и редактора кода.
5. Создайте структуру папок D:\Dev\cpp\system_prog для будущих проектов курса.

Примечание: Это окружение станет фундаментом для всех последующих экспериментов с кодом, компиляцией и отладкой.

---

Занятие П2. Мастерство компиляции и сборки нативного кода

Цель: Научиться управлять процессом компиляции и сборки нативного C/C++ кода в Windows, понимать ключевые параметры компилятора и создавать отлаживаемые исполняемые файлы.

Ключевые темы:

· Visual Studio как набор инструментов (Toolchain): Установка рабочей нагрузки "Разработка классических приложений на C++". Понимание роли cl.exe (компилятор), link.exe (компоновщик), lib.exe и nmake.exe.
· Компиляция из командной строки: Запуск Developer Command Prompt или Developer PowerShell. Написание и компиляция простейшей программы "Hello, World" с помощью cl и link.
· Ключевые флаги компилятора MSVC:
  · /DEBUG - Генерация отладочной информации. Важность флагов /DEBUG:FULL (для релизных билдов) и /DEBUG:FASTLINK (для быстрой локальной разработки).
  · /Od, /O1, /O2 - Управление оптимизацией (отключение, оптимизация по размеру, по скорости).
  · /W4, /WX - Уровни предупреждений и трактовка предупреждений как ошибок.
· Работа с отладочной информацией (PDB-файлы): Что такое PDB-файл, почему он важен для отладки, где он создается. Связь исполняемого файла (*.exe) и его PDB.
· Статическая и динамическая компоновка: Различия между /MT, /MTd, /MD, /MDd. Работа со статическими (*.lib) и динамическими (*.dll) библиотеками.

Практическое задание:

1. Скомпилируйте "Hello, World" из командной строки, явно указав компилятору и компоновщику имена исходного и конечного файлов.
2. Создайте две версии одной программы: одну с флагом /Od /DEBUG:FASTLINK, другую с флагом /O2 /DEBUG:FULL. Сравните размеры полученных .exe и .pdb файлов.
3. Напишите программу, использующую функцию из стандартной библиотеки C (printf, malloc). Соберите её, попробовав разные флаги компоновки (/MT и /MD). Проанализируйте размер итогового исполняемого файла и его зависимости (можно с помощью dumpbin /dependents).

Примечание: Понимание этих этапов необходимо для диагностики проблем со сборкой, управления производительностью и, что критично для системного программирования, для последующей эффективной отладки.

---

Занятие П3. Введение в отладку и системное профилирование

Игорь, [06.01.2026 19:05]
Цель: Освоить базовый инструментарий для диагностики проблем, анализа производительности и исследования поведения приложений и самой системы Windows.

Ключевые темы:

· Системный монитор (Perfmon): Введение в счетчики производительности. Создание набора сборщиков данных для мониторинга CPU (% Processor Time), памяти (Available MBytes, Page Faults/sec), диска и сети.
· Мощь ETW и Windows Performance Toolkit: Запись системной трассировки с помощью Windows Performance Recorder (WPR). Базовый анализ в Windows Performance Analyzer (WPA): просмотр активности CPU, дискового ввода-вывода, работы планировщика.
· Инструменты Sysinternals: Знакомство с набором утилит от Марка Руссиновича. Базовое использование Process Explorer (продвинутый диспетчер задач), Process Monitor (мониторинг файловой системы, реестра, процессов), Autoruns (управление автозагрузкой).
· Создание и анализ дампов памяти: Использование Procdump для захвата дампа процесса по условию (высокая загрузка CPU, необработанное исключение). Базовое открытие дампа в WinDbg и выполнение простых команд (!analyze -v, k для вывода стека).
· Отладка "на лету" с WinDbg: Присоединение к живому процессу. Установка точек останова, пошаговое выполнение, просмотр значений переменных и регистров.

Практическое задание:

1. Запустите Perfmon и добавьте ключевые счетчики для мониторинга системы. Запустите тяжелое приложение (например, архиватор) и понаблюдайте за графиками.
2. Запишите короткую трассировку WPR во время запуска и работы любого приложения. Откройте ее в WPA и найдите на графике CPU активность вашего процесса.
3. Запустите Process Monitor, настройте фильтр для отображения операций только одного выбранного процесса (например, notepad.exe). Посмотрите, к каким файлам и разделам реестра он обращается.
4. Напишите простую C++ программу, которая уходит в бесконечный цикл. С помощью Procdump захватите дамп этого процесса. Откройте дамп в WinDbg и выполните команду !analyze -v.

Примечание: Эти инструменты являются "швейцарским армейским ножом" системного программиста и администратора. Умение ими пользоваться напрямую пригодится при изучении процессов, потоков, памяти и вводов-вывода из основного курса.

---

Эти три урока можно разместить в самом начале вашего плана, перед Частью 1: Основы системного программирования.
